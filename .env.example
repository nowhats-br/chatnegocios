# ChatNegocios — exemplo de configuração

# Backend Express
PORT=3001
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/chatnegocios
CORS_ORIGINS=http://localhost:5173

# Frontend (Vite)
VITE_BACKEND_URL=http://localhost:3001

# Evolution API (provedor)
VITE_EVOLUTION_API_URL=http://localhost:8080
VITE_EVOLUTION_API_KEY=change-me
# Opcional: endpoints específicos do seu provedor
VITE_EVOLUTION_QR_ENDPOINT_TEMPLATE=/instance/qrCode/{instanceName}
# Webhook público para receber eventos (em dev pode ficar vazio)
VITE_EVOLUTION_WEBHOOK_URL=

# Atualização via UI (admin)
# Habilite apenas se você deseja que o admin aplique updates pelo frontend.
ENABLE_UI_UPDATE=false
# Formato: owner/repo (ex.: nowhats-br/chatnegocios)
GITHUB_REPO=
# Branch padrão para checagens e reset hard
GITHUB_BRANCH=main
# Reiniciar automaticamente o processo Node após aplicar atualização
AUTO_RESTART_ON_UPDATE=false

# Observações:
# - Reinicie o dev server após alterar `.env` (npm run dev).
# - Para produção bare‑metal com Nginx/SSL, use `scripts/install-baremetal.sh`.
#   Ele cria `.env`, configura Nginx e obtém certificados com o Certbot.