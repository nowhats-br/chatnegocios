# Evolution API Configuration
VITE_EVOLUTION_API_URL=https://api.seu-dominio.com
VITE_EVOLUTION_API_KEY=your_evolution_api
## Opcional: template do endpoint de QR; use {instanceName} como placeholder
## Exemplos: /instance/qrCode/{instanceName} ou /instance/connect/{instanceName}/qrcode
VITE_EVOLUTION_QR_ENDPOINT_TEMPLATE=
## Opcional: URL pública para receber webhooks (não use localhost)
## Exemplo: https://chat.seu-dominio.com/api/evolution/webhook ou URL de função serverless
VITE_EVOLUTION_WEBHOOK_URL=
## Não use a URL do provedor como webhook (ex.: https://api.seu-dominio.com/api/evolution/webhook)
 
## Observação: autenticação e dados usam apenas Express + PostgreSQL (sem Supabase).

# Overrides dos endpoints da Evolution API (opcionais)
# Permite adaptar rotas conforme seu provedor sem alterar código.
# Use caminhos relativos à base em VITE_EVOLUTION_API_URL.
VITE_EVOLUTION_INSTANCE_CREATE_ENDPOINT=/instance/create
VITE_EVOLUTION_INSTANCE_CONNECT_ENDPOINT=/instance/connect/{instanceName}
VITE_EVOLUTION_INSTANCE_QR_ENDPOINT=/instance/qrCode/{instanceName}
VITE_EVOLUTION_INSTANCE_STATUS_ENDPOINT=/instance/fetchInstances/{instanceName}
VITE_EVOLUTION_INSTANCE_DELETE_ENDPOINT=/instance/delete/{instanceName}
VITE_EVOLUTION_SEND_TEXT_ENDPOINT=/message/sendText
VITE_EVOLUTION_SEND_MEDIA_ENDPOINT=/message/sendMedia

# Dica: se seu provedor exigir Bearer Token, salve VITE_EVOLUTION_API_KEY como
# "Bearer SEU_TOKEN_AQUI". Caso contrário, apenas a chave será enviada via header "apikey".