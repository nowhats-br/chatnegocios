# ChatNegocios — exemplo de configuração de ambiente
# Copie este arquivo para `.env` e ajuste os valores conforme seu ambiente.

# Backend (Express)
VITE_BACKEND_URL=http://localhost:3001
PORT=3001
CORS_ORIGINS=http://localhost:5173

# Evolution API (provedor)
# Se você usa Traefik/Portainer conforme os scripts deste projeto,
# pode acessar via http://evolution.localtest.me
VITE_EVOLUTION_API_URL=http://localhost:8080
VITE_EVOLUTION_API_KEY=changeme

# QR Code — template de endpoint (opcional)
# Exemplos: /instance/qrCode/{instanceName} ou /instance/connect/{instanceName}/qrcode
VITE_EVOLUTION_QR_ENDPOINT_TEMPLATE=/instance/qrCode/{instanceName}

# Webhook público para receber eventos (opcional em dev)
# Em desenvolvimento, use um túnel (ngrok/Cloudflared) para um endereço público.
# Exemplo: https://seu-tunel.ngrok.app/api/evolution/webhook
VITE_EVOLUTION_WEBHOOK_URL=

# Banco de dados Postgres — se não definido, backend usa DB em memória
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/chatnegocios
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=chatnegocios

# Produção com SSL (Traefik + Let's Encrypt)
# Preencha ao usar o instalador Ubuntu
ACME_EMAIL=
CHATNEGOCIOS_DOMAIN=
CHATNEGOCIOS_API_DOMAIN=
EVOLUTION_DOMAIN=

# CORS (produção)
# Liste origens permitidas separadas por vírgula. Exemplo:
# CORS_ORIGINS=https://seu-dominio-chatnegocios.com,https://outro-dominio.com
# O instalador Ubuntu v2 preenche automaticamente com o domínio do frontend.
# Em dev, mantenha http://localhost:5173.
CORS_ORIGINS=

# Observações:
# - Supabase foi removido deste projeto; variáveis antigas de Supabase podem ser apagadas.
# - Reinicie o dev server após alterar `.env` (npm run dev).